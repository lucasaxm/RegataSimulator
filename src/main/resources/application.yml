spring:
  application:
    name: bilubot
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health
jasypt:
  encryptor:
    password: ${REGATA_SIMULATOR_ENC_PASSWORD}